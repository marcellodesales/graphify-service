name: 🐳 docker-multiarch-cicd (worker)
run-name: 🐳 worker multiarch — PR ${{ github.event.pull_request.number }} ${{ github.event.pull_request.head.sha }}

# Builds the graphify-worker image (backend/worker) via the shared vionix
# reusable workflow. The worker image is built FROM the graphify image, so the
# `graphify` binary is available at runtime. Path-filtered to the worker
# component + shared build inputs.
#
# ⚠️ Image-naming caveat: the vionix reusable workflow names images by REPO, not
# by target-service (see the "vionix-docker-cicd" skill). Distinct per-service
# images from this monorepo require enhancing the reusable workflow to append
# /${target-service} to the image base (vionix-proj/github-platform follow-up).

on:
  pull_request:
    types: [opened, synchronize, reopened, closed]
    paths:
      - '.github/workflows/docker-multiarch-cicd-worker.yaml'
      - 'backend/worker/**'
      - 'backend/internal/**'
      - 'backend/go.mod'
      - 'backend/go.sum'
      - 'docker-compose*'
      - '.dockerignore'
  push:
    branches:
      - main
      - develop
    tags:
      - 'v*'
    paths:
      - '.github/workflows/docker-multiarch-cicd-worker.yaml'
      - 'backend/worker/**'
      - 'backend/internal/**'
      - 'backend/go.mod'
      - 'backend/go.sum'
      - 'docker-compose*'
      - '.dockerignore'

env:
  DOCKERHUB_USERNAME: ${{ secrets.DOCKER_REGISTRY_USERNAME }}
  DOCKERHUB_TOKEN: ${{ secrets.DOCKER_REGISTRY_TOKEN }}

jobs:
  setup:
    name: 🔧 setup
    runs-on: ubuntu-latest
    outputs:
      dockerhub-token: ${{ steps.secrets.outputs.token }}
      dockerhub-username: ${{ steps.secrets.outputs.username }}
    steps:
      - name: Load secrets
        id: secrets
        run: |
          echo "username=${{ secrets.DOCKER_REGISTRY_USERNAME }}"
          echo "token=${{ secrets.DOCKER_REGISTRY_TOKEN }}"

  docker-multiarch:
    name: 🐳 docker (worker)
    needs: [ setup ]
    uses: vionix-proj/github-platform/.github/workflows/docker-multiarch-cicd.yaml@main
    permissions:
      contents: write
      packages: write
      id-token: write
      attestations: write
      artifact-metadata: write
      pull-requests: write
    with:
      registry: ghcr.io
      platforms: linux/amd64,linux/arm64
      docker-compose-target-service: graphify-worker
      # Publishes ghcr.io/<owner>/graphify-service/worker (monorepo suffix).
      image-name-suffix: worker
      docker-compose-file-name: docker-compose.yaml
      dockerhub-registry: docker.io
      dockerhub-namespace: ${{ github.repository_owner }}
      dockerhub-registry-username: ${{ needs.setup.outputs.dockerhub-username }}
      dockerhub-registry-token: ${{ needs.setup.outputs.dockerhub-token }}
      push-attestations: "false"
